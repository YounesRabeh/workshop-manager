{
  "name": "workshop-manager",
  "productName": "Workshop Manager",
  "version": "1.3.7",
  "description": "Create and update items for Steam's Workshop.",
  "author": "Younes Rabeh",
  "license": "MIT",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.4.0",
  "main": "out/main/index.js",
  "build": {
    "appId": "com.workshop.manager",
    "productName": "Workshop Manager",
    "directories": {
      "output": "dist"
    },
    "beforeBuild": "./scripts/build/electron-builder-before-build.mjs",
    "afterSign": "./scripts/build/after-sign.mjs",
    "asar": true,
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "files": [
      "out/**/*",
      "resources/img/**/*",
      "package.json"
    ],
    "extraMetadata": {
      "main": "out/main/index.js"
    },
    "win": {
      "icon": "resources/img/app-icon.ico",
      "target": [
        {
          "target": "nsis"
        }
      ]
    },
    "nsis": {
      "include": "resources/installer.nsh",
      "runAfterFinish": true
    },
    "mac": {
      "icon": "resources/img/app-icon.icns",
      "target": [
        {
          "target": "dmg"
        }
      ]
    },
    "linux": {
      "icon": "resources/img/app-icon.png",
      "category": "Utility",
      "desktop": {
        "entry": {
          "Name": "Workshop Manager",
          "StartupWMClass": "workshop-manager"
        }
      },
      "target": [
        {
          "target": "AppImage"
        }
      ]
    }
  },
  "scripts": {
    "clean": "node scripts/maintenance/clean.mjs",
    "kill:instance": "node scripts/runtime/kill-old-instance.mjs",
    "predev": "pnpm sync:icon && pnpm kill:instance",
    "dev": "env -u ELECTRON_RUN_AS_NODE CHROME_DESKTOP=workshop-manager.desktop electron-vite dev",
    "dev:icon": "pnpm sync:icon && pnpm dev",
    "sync:icon": "node scripts/build/sync-app-icon.mjs",
    "release:checksums": "node scripts/build/generate-release-checksums.mjs",
    "build:bundle:native": "pnpm sync:icon && electron-vite build",
    "build:bundle": "pnpm sync:icon && node scripts/build/run-build-in-docker.mjs build:bundle:native",
    "build": "pnpm sync:icon && node scripts/build/run-build-in-docker.mjs build:bundle:native",
    "build:exe:native": "pnpm sync:icon && node scripts/build/build-executable.mjs",
    "build:linux": "pnpm sync:icon && node scripts/build/run-build-in-docker.mjs build:exe:native",
    "build:linux:checksums": "pnpm sync:icon && pnpm build:linux && pnpm release:checksums",
    "build:linux:icon": "pnpm sync:icon && node scripts/build/run-build-in-docker.mjs build:exe:native --generate-icon",
    "build:linux:icon:checksums": "pnpm sync:icon && pnpm build:linux:icon && pnpm release:checksums",
    "build:win": "pnpm sync:icon && node scripts/build/run-build-in-docker.mjs build:exe:native --win",
    "build:win:checksums": "pnpm sync:icon && pnpm build:win && pnpm release:checksums",
    "build:win:icon": "pnpm sync:icon && node scripts/build/run-build-in-docker.mjs build:exe:native --win --generate-icon",
    "build:win:icon:checksums": "pnpm sync:icon && pnpm build:win:icon && pnpm release:checksums",
    "build:all:icon": "pnpm sync:icon && pnpm build:linux:icon && pnpm build:win:icon",
    "build:all:icon:checksums": "pnpm sync:icon && pnpm build:all:icon && pnpm release:checksums",
    "release": "pnpm sync:icon && pnpm build:all:icon && pnpm release:checksums",
    "preview": "env -u ELECTRON_RUN_AS_NODE CHROME_DESKTOP=workshop-manager.desktop electron-vite preview",
    "preview:icon": "pnpm sync:icon && pnpm preview",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && vue-tsc --noEmit -p tsconfig.renderer.json && vue-tsc --noEmit -p tsconfig.renderer-tests.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "extract-zip": "^2.0.1",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.2",
    "@types/node": "^22.13.14",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/test-utils": "^2.4.6",
    "autoprefixer": "^10.4.20",
    "electron": "^35.0.0",
    "electron-builder": "^26.8.1",
    "electron-vite": "^3.0.0",
    "jsdom": "^26.0.0",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.2",
    "typescript": "^5.8.2",
    "vite": "^6.2.1",
    "vitest": "^3.0.9",
    "vue-tsc": "^2.2.8"
  }
}
