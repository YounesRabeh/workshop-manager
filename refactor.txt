Refactoring Plan for useAuthFlow (679 lines):

Extract Error Handling → Create useAuthErrorUtils.ts:

Move parseAndLogError, normalizeError, toAuthIssue, isSavedSessionFallbackError, isSteamCmdMissingConfigurationMessage, handleSteamCmdMissingStatus
Extract Form State Management → Create useLoginForm.ts:

Move all loginForm reactive state and setters (setLoginUsername, setLoginPassword, etc.)
Move refreshRememberedLoginState
Extract Steam Guard Logic → Create useSteamGuard.ts:

Move Steam Guard state (steamGuardCode, steamGuardPromptType, etc.)
Move applyOtpChallengeStatus, applyMobileChallengeStatus, submitSteamGuardCode, submitQueuedOtpIfReady
Extract Profile Management → Create useSteamProfile.ts:

Move accountPersonaName, accountProfileImageUrl, refreshCurrentProfile
Simplify Main Composable → Keep useAuthFlow as orchestrator:

Import and compose the sub-composables
Keep only login/logout orchestration and run event handling
Refactoring Plan for SteamCmdProcessSession (930 lines):

Extract Process Management → Create SteamCmdProcessManager.ts:

Move process spawning, options building, invalidation logic
Extract Run Handling → Create SteamCmdRunHandler.ts:

Move run preparation, timeout management, active run creation
Extract Output Processing → Create SteamCmdOutputProcessor.ts:

Move line processing, prompt detection, result parsing
Extract Logging → Create SteamCmdLogger.ts:

Move log persistence and run event emission
Refactor Main Class → Keep SteamCmdProcessSession as coordinator:

Compose the extracted modules
Keep high-level orchestration methods

